# DaVal Platform

A specialized automation SaaS platform for SMBs.

## Directory Structure
- `/client`: React (Vite) frontend with Tailwind CSS.
- `/api`: Node.js (Express) backend for Vercel Serverless Functions.

## Development
```bash
# Frontend
cd client
npm run dev

# Backend (local testing)
cd api
node index.js
```
